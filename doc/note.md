# Study Notes

## The Interfaces of P2P Component

```c
create_instance();
create_session(offer/answer, on_message_callback);
get_local_sdp();
set_remote_sdp();
send_message();
destroy_session();
destroy_instance();
```
